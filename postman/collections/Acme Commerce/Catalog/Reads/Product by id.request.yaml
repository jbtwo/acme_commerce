$kind: http-request
name: Product by id
url: "{{base_url}}/api/v1/products/{{seed_product_id}}"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('found the seeded product', () => {
        pm.response.to.have.status(200);
        pm.expect(pm.response.json().data.title).to.eql('Trailhead 30L Backpack');
      });
    language: text/javascript
order: 2287187551070472
