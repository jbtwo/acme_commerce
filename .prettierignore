dist
node_modules
coverage
openapi/openapi.json
package-lock.json

# Generated and owned by Postman's git integration, not by this repository. Prettier and
# Postman would otherwise reformat these files in a loop on every sync, and the diff noise
# would obscure the changes you actually made to a request.
postman/
.postman/

# Process documents you author and regenerate, not project documentation. Left alone so
# Prettier does not rewrite your prose on every commit. LEARNING_PLAN_AUDIT.md additionally
# cannot be formatted idempotently — Prettier rewrites it on every pass, so `format:check`
# would fail on it forever.
docs/LEARNING_PLAN_AUDIT.md
docs/REVISION_PROMPT.md
